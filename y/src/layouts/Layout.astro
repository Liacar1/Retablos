---
import '../styles/global.css';
const year = new Date().getFullYear();
const base = Astro.site ? new URL(Astro.site).pathname : '/';
const base1 = '/Retablos/';
---

<!doctype html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <title>Taller de Retablos - Lía Carvajal</title>
    <script defer src="https://unpkg.com/alpinejs@3.x.x/dist/cdn.min.js"></script>
  </head>

  <body class="bg-gradient-to-br from-orange-50/80 via-amber-50/80 to-yellow-50/80 min-h-screen flex flex-col backdrop-blur-sm">

    <!-- Header / Navbar con diseño mejorado -->
    <header class="bg-gradient-to-r from-orange-400/90 to-amber-500/90 text-white shadow-2xl sticky top-0 z-50 backdrop-blur-sm border-b border-orange-300/50">
      <nav class="max-w-7xl mx-auto px-6 py-4 flex justify-between items-center" x-data="{ open: false }">
        <!-- Logo y título a la izquierda -->
        <a href="/" class="text-xl md:text-2xl font-bold hover:text-orange-200 transition duration-300 flex items-center gap-3">
        <img 
          src={`${base1}cadimalogo.PNG`} 
          alt="Logo CADIMA Centro Artesanal Divino Maestro" 
          class="w-32 h-16 object-contain drop-shadow-lg"
  />
          Taller de Retablos - Lía Carvajal
        </a>
        
        <!-- Menú de navegación al lado derecho -->
        <div class="hidden md:flex items-center space-x-6">
      <a href={base} class="hover:text-orange-200 font-semibold transition duration-300 flex items-center gap-2 hover:bg-white/20 p-2 rounded-lg">
        <span>🏠</span>
        Inicio
      </a>
      <a href={`${base}info`} class="hover:text-orange-200 font-semibold transition duration-300 flex items-center gap-2 hover:bg-white/20 p-2 rounded-lg">
        <span>📝</span>
        Inscripciones
      </a>
      <a href={`${base}productos`} class="hover:text-orange-200 font-semibold transition duration-300 flex items-center gap-2 hover:bg-white/20 p-2 rounded-lg">
        <span>🛍️</span>
        Productos
      </a>
      <a href={`${base}#contacto`} class="hover:text-orange-200 font-semibold transition duration-300 flex items-center gap-2 hover:bg-white/20 p-2 rounded-lg">
        <span>📞</span>
        Contactos
      </a>
    </div>

        
        <!-- Botón hamburger para móvil -->
        <button @click="open = !open" class="md:hidden focus:outline-none bg-white/20 p-2 rounded-lg hover:bg-white/30 transition-colors">
          <svg class="w-6 h-6" xmlns="http://www.w3.org/2000/svg" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
            <path x-show="!open" stroke-linecap="round" stroke-linejoin="round" d="M4 6h16M4 12h16M4 18h16"/>
            <path x-show="open" stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12"/>
          </svg>
        </button>

        <!-- Menú móvil -->
        
                
        <div 
          class="absolute top-full left-0 w-full bg-gradient-to-br from-orange-400/95 to-amber-500/95 px-6 py-4 flex-col gap-4 backdrop-blur-sm border-b border-orange-300/50"
          :class="{ 'flex': open, 'hidden': !open }"
        >
          <a href={base} class="hover:text-orange-200 font-semibold transition duration-300 flex items-center gap-2 hover:bg-white/20 p-2 rounded-lg">
            <span>🏠</span>
            Inicio
          </a>
          <a href={`${base}info`} class="hover:text-orange-200 font-semibold transition duration-300 flex items-center gap-2 hover:bg-white/20 p-2 rounded-lg">
            <span>📝</span>
            Inscripciones
          </a>
          <a href={`${base}productos`} class="hover:text-orange-200 font-semibold transition duration-300 flex items-center gap-2 hover:bg-white/20 p-2 rounded-lg">
            <span>🛍️</span>
            Productos
          </a>
          <a href={`${base}#contacto`} class="hover:text-orange-200 font-semibold transition duration-300 flex items-center gap-2 hover:bg-white/20 p-2 rounded-lg">
            <span>📞</span>
            Contactos
          </a>
        </div>


      </nav>

      <style>
        .hover-underline-animation {
          position: relative;
          display: inline-block;
        }

        .hover-underline-animation::after {
          content: '';
          position: absolute;
          width: 100%;
          transform: scaleX(0);
          height: 2px;
          bottom: -2px;
          left: 0;
          background-color: #fbbf24;
          transform-origin: bottom right;
          transition: transform 0.25s ease-out;
        }

        .hover-underline-animation:hover::after {
          transform: scaleX(1);
          transform-origin: bottom left;
        }
      </style>
    </header>

    <!-- Main content con fondo de imagen restaurado -->
    <main class="w-full min-h-screen px-6 py-10 space-y-10 bg-[url('/fondo2.jpg')] bg-cover bg-center bg-no-repeat bg-fixed">
      <slot />
    </main>

    <!-- Footer mejorado -->
    <footer class="bg-gradient-to-r from-orange-100/80 to-amber-100/80 text-center py-6 text-slate-700 font-semibold mt-auto backdrop-blur-sm border-t border-orange-200/50">
      <div class="max-w-7xl mx-auto px-6">
        <div class="flex flex-col md:flex-row justify-between items-center gap-4">
          <div class="flex items-center gap-3">
            <span class="bg-orange-500 text-white p-2 rounded-full">📞</span>
            <span class="text-lg font-bold">© {year} Lía Carvajal</span>
          </div>
          <div class="flex items-center gap-4 text-sm">
            <span class="flex items-center gap-2">
              <span>📧</span>
              <span>Arte Manual y Decorativo</span>
            </span>
            <span class="flex items-center gap-2">
              <span>📍</span>
              <span>La Paz, Bolivia</span>
            </span>
          </div>
        </div>
      </div>
    </footer>

  </body>
</html>